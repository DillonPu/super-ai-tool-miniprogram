const app = getApp();
Page({

  /**
   * 页面的初始数据
   */
  data: {
    msgAnswerType:"3",
    defaultFirstMsg:"不管你怎么问，注意，我就是杠精"
  },

  /**
   * 生命周期函数--监听页面加载
   */
  onLoad(options) {
   
  },

  /**
   * 生命周期函数--监听页面初次渲染完成
   */
  onReady: function (options) {
 },

  /**
   * 生命周期函数--监听页面显示
   */
  onShow() {
    // app.pageBindOpEvent(this.onConnect, this.onMessage);
  },

    /**
   * 生命周期函数--监听页面卸载
   */
  onUnload() {
   
  }
})
